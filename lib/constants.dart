import 'package:flutter/material.dart';

const primaryColor = Colors.deepPurple;
const bgColor = Colors.grey;
const welcomeBgColor = Color.fromRGBO(180, 198, 240, 1);

const appIcon = Icon(Icons.phone_android, size: 100, color: primaryColor);
